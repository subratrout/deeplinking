<%= form_for(@parsing) do |f| %>
  <% if @parsing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parsing.errors.count, "error") %> prohibited this parsing from being saved:</h2>

      <ul>
      <% @parsing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_area :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
